# PR Doctor 🩺 — Agentic DevOps PR Reviewer + Auto-Fix

PR Doctor is an agentic DevOps assistant that:
- analyzes GitHub Pull Requests for high-risk issues (secrets, debug logs, SQL injection patterns, missing tests),
- generates a safe patch (unified diff),
- posts a structured review comment on the PR,
- and can open an auto-fix PR with the changes.

🏷️ Badges:
<p align="left">
  <img src="https://img.shields.io/badge/build-passing-brightgreen" />
  <img src="https://img.shields.io/badge/license-MIT-blue" />
  <img src="https://img.shields.io/badge/backend-FastAPI-009688" />
  <img src="https://img.shields.io/badge/frontend-React%20%2B%20Vite-61DAFB" />
  <img src="https://img.shields.io/badge/infra-Azure%20Container%20Apps-0078D4" />
</p>

## Live Demo Proof (GitHub)
- Demo repo: https://github.com/M10vir/pr-doctor-demo-repo
- Bad PR: https://github.com/M10vir/pr-doctor-demo-repo/pull/1
- Auto-fix PR (generated by PR Doctor): https://github.com/M10vir/pr-doctor-demo-repo/pull/4

## What it does (end-to-end)
1) Create a Run (stores PR URL + status)
2) Analyze PR (heuristics + file/line hints)
3) Generate Patch (safe unified diff)
4) Comment Review on PR (agent action proof)
5) Open Fix PR (branch + commit + PR)

## Tech Stack
**Backend**
- FastAPI (Python 3.11)
- httpx (GitHub REST API)
- SQLAlchemy (run history)
- python-dotenv (local env loading)

**Frontend**
- React + Vite
- TailwindCSS

**Deploy**
- Azure Container Apps (Backend API)
- (Recommended) Azure Static Web Apps (Frontend)

🧱 Architecture Diagram:

                         ┌──────────────────────────┐
                         │      GitHub Pull Request │
                         └──────────────┬───────────┘
                                        │
                                        ▼
                         ┌──────────────────────────┐
                         │      PR Doctor Backend   │
                         │        (FastAPI)         │
                         └──────────────┬───────────┘
                                        │
        ┌───────────────────────────────┼───────────────────────────────┐
        │                               │                               │
        ▼                               ▼                               ▼
┌────────────────┐           ┌────────────────────┐          ┌────────────────────┐
│  Analyzer      │           │ Patch Generator    │          │ GitHub API Client  │
│ (Heuristics)   │           │ (Unified Diff)     │          │ (httpx)            │
└────────────────┘           └────────────────────┘          └────────────────────┘
                                        │
                                        ▼
                         ┌──────────────────────────┐
                         │   GitHub Actions / PRs   │
                         │  - Review Comments       │
                         │  - Auto-Fix PRs          │
                         └──────────────────────────┘


---

# Local Run

## Backend (FastAPI)
> Local secrets go in `backend/.env` (do not commit).

Create `backend/.env`:
```bash
GITHUB_TOKEN=ghp_xxx
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:4173

Run:

cd backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python -m uvicorn app.main:app --reload --port 8000

Test:

curl -s http://localhost:8000/health

Frontend (React)

Create frontend/.env:

VITE_API_BASE=http://localhost:8000

Run:

cd frontend
npm install
npm run dev


⸻

Azure Deploy (Backend) — Azure Container Apps

IMPORTANT: .env files are local only. Azure requires setting env vars/secrets on the Container App.

flowchart TB

    subgraph Azure["Microsoft Azure"]
        
        subgraph ACA["Azure Container Apps"]
            Backend[PR Doctor Backend<br/>(FastAPI Container)]
        end

        subgraph SWA["Azure Static Web Apps"]
            Frontend[React + Vite Frontend]
        end

        subgraph ACR["Azure Container Registry"]
            Images[Backend Container Image]
        end

        subgraph Logs["Log Analytics Workspace"]
            LogsDB[Diagnostics + Metrics]
        end
    end

    GitHub[GitHub Repos + PRs] --> Backend
    Frontend --> Backend
    Backend --> LogsDB
    Backend --> Images

1) Create Resource Group + ACR

export RG="pr-doctor-rg"
export LOC="eastus"
az group create -n $RG -l $LOC

export ACR_NAME="prdoctoracr$RANDOM"
az acr create -g $RG -n $ACR_NAME --sku Basic
az acr login -n $ACR_NAME

2) Build & push backend image

From repo root:

az acr build -r $ACR_NAME -t pr-doctor-backend:v1 ./backend

3) Create Container Apps environment

export ENV="pr-doctor-env"
az containerapp env create -n $ENV -g $RG -l $LOC

4) Create backend Container App

export APP="pr-doctor-api"
az containerapp create \
  -n $APP \
  -g $RG \
  --environment $ENV \
  --image $ACR_NAME.azurecr.io/pr-doctor-backend:v1 \
  --registry-server $ACR_NAME.azurecr.io \
  --target-port 8000 \
  --ingress external \
  --query properties.configuration.ingress.fqdn -o tsv

Set:

export API_FQDN="<FQDN_FROM_ABOVE>"
export API_URL="https://$API_FQDN"

5) Set GitHub token as a secret (required)

az containerapp secret set \
  -n $APP -g $RG \
  --secrets github-token="<PASTE_GITHUB_PAT>"

az containerapp update \
  -n $APP -g $RG \
  --set-env-vars \
    GITHUB_TOKEN=secretref:github-token \
    ALLOWED_ORIGINS="http://localhost:5173,http://localhost:4173"

6) Verify

curl -i -L $API_URL/health


⸻

Frontend Deploy (Recommended)

Deploy frontend/ to Azure Static Web Apps and set:
	•	VITE_API_BASE=https://<your-container-app-fqdn>

Then update backend:
	•	ALLOWED_ORIGINS=https://<your-frontend-domain>,http://localhost:5173,http://localhost:4173

⸻

Requirements

Backend (backend/requirements.txt):
	•	fastapi==0.115.0
	•	uvicorn[standard]==0.30.6
	•	python-dotenv==1.0.1
	•	httpx==0.27.2
	•	pydantic==2.8.2
	•	sqlalchemy==2.0.32

---

Thank you for helping make PR Doctor better!
 
